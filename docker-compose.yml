name: kurdischesfestival

services:
  app:
    build:
      context: ./server
    env_file:
      - .env
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - default
      - proxy
    expose:
      - "3000"
    environment:
      - CANONICAL_HOST=www.kurdischesfilmfestival.komplettwebdesign.de
    volumes:
      - /home/webadmin/apps/kurdischesfestival/data/uploads:/app/public/uploads
      - /home/webadmin/apps/kurdischesfestival/data/downloads:/app/downloads
     # - ./data/uploads:/app/public/uploads
     # - ./data/downloads:/app/downloads
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.services.kff-app.loadbalancer.server.port=3000"
      - "traefik.http.routers.kff-www.rule=Host(`${SITE_HOST_WWW}`)"
      - "traefik.http.routers.kff-www.entrypoints=websecure"
      - "traefik.http.routers.kff-www.tls.certresolver=le"
      - "traefik.http.routers.kff-www.service=kff-app"
      - "traefik.http.routers.kff-www.priority=1000"
      - "traefik.http.routers.kff-root.priority=1000"
      - "traefik.http.routers.kff-root.rule=Host(`${SITE_HOST_ROOT}`)"
      - "traefik.http.routers.kff-root.entrypoints=websecure"
      - "traefik.http.routers.kff-root.tls.certresolver=le"
      - "traefik.http.routers.kff-root.middlewares=kff-to-www"
      - "traefik.http.routers.kff-root.service=kff-app"
      - "traefik.http.middlewares.kff-to-www.redirectregex.regex=^https?://${SITE_DOMAIN}/(.*)"
      - "traefik.http.middlewares.kff-to-www.redirectregex.replacement=https://${SITE_HOST_WWW}/$$1"
      - "traefik.http.middlewares.kff-to-www.redirectregex.permanent=true"

  postgres:
    image: pgvector/pgvector:pg16
    env_file:
      - .env
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - /home/webadmin/apps/kurdischesfestival/data/postgres:/var/lib/postgresql/data
     # - ./data/postgres:/var/lib/postgresql/data
    networks:
      default: {}
      dbadmin:
        aliases:
          - kff-postgres
    # VPN-only Zugriff von außen:
    ports:
      - "10.10.0.1:5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10

  webhook:
    build:
      context: ./deploy/webhook
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - proxy
    expose:
      - "4000"
    volumes:
      - /home/webadmin/apps:/apps:rw
      - /var/run/docker.sock:/var/run/docker.sock:rw
      - /home/webadmin/.ssh/github_ionos_ed25519:/root/.ssh/id_ed25519:ro
    environment:
      - WEBHOOK_PORT=4000
      - DEPLOY_SCRIPT=/apps/kurdischesfestival/deploy/deploy.sh
      - DEPLOY_BRANCH=${DEPLOY_BRANCH}
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.routers.kff-deploy.rule=Host(`${HOOKS_HOST}`) && PathPrefix(`/kurdischesfestival`)"
      - "traefik.http.routers.kff-deploy.entrypoints=websecure"
      - "traefik.http.routers.kff-deploy.tls.certresolver=le"
      - "traefik.http.routers.kff-deploy.priority=1000"
      - "traefik.http.routers.kff-deploy.middlewares=kff-deploy-strip"
      - "traefik.http.middlewares.kff-deploy-strip.stripprefix.prefixes=/kurdischesfestival"
      - "traefik.http.services.kff-deploy.loadbalancer.server.port=4000"

networks:
  proxy:
    external: true
  dbadmin:
    external: true
